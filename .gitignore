# Nothing here is under version control yet -- this file is ready for the day
# `git init` happens, so the first commit does not sweep in build output.

# Secrets and machine-local overrides. .env.example is committed; .env is not.
.env

# Node
node_modules/
dist/
*.log

# Gradle / Java
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# Editors and OS
.idea/
.vscode/
*.iml
.DS_Store

# Internal notes and plans. Kept local, not published to the remote.
docs/

# graphify knowledge graph. Derived output, rebuilt by `make graph-update`, and
# it records absolute host paths (.graphify_python, .graphify_root) that are
# wrong on any other machine. CLAUDE.md and .claude/settings.json ARE tracked --
# they are the instructions that point at this directory.
graphify-out/
